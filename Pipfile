[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
numpy = ">=1.21.0"
scipy = ">=1.7.0"
pandas = ">=1.3.0"
matplotlib = ">=3.4.0"
scikit-learn = ">=0.24.0"
sweetviz = ">=2.1.3"
streamlit = ">=1.28.0"
plotly = ">=5.13.0"
alpaca-trade-api = ">=3.0.0"
python-dotenv = ">=0.19.0"
ta = ">=0.10.0"
talib-binary = ">=0.4.24"
schedule = ">=1.1.0"
cryptography = ">=41.0.0"
python-jose = ">=3.3.0"

[dev-packages]

[requires]
python_version = "3.9"
